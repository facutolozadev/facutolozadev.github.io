---
const { sx } = Astro.props;
---

<button
  class={`px-12 py-4 bg-accent text-black hover:scale-110 transition-all duration-500 hiddenElement ${sx}`}
>
  <h2 class="font-bold">
    <slot />
  </h2>
</button>
